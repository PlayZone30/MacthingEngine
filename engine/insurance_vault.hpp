#pragma once
// InsuranceVault methods are defined inline in models.hpp (struct InsuranceVault).
// This header exists as a placeholder for V1.1 extensions (per-instrument vaults,
// withdrawal gate, depositor tracking).
#include "models.hpp"
